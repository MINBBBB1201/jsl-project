# 파트너 로고 SVG 는 출처(Wikimedia)에서 받은 원본 그대로여야 한다.
# core.autocrlf=true 환경에서 체크아웃하면 줄바꿈이 CRLF 로 바뀌어 원본과
# 바이트가 달라지고, "출처와 대조해 검증한다"는 전제가 깨진다.
# -text 로 줄바꿈 변환 대상에서 뺀다 (렌더 결과와는 무관하지만 검증 가능성 문제다).
frontend/public/logos/** -text
